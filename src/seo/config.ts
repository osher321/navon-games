/**
 * The site isn't deployed to a permanent domain yet, so this is a
 * placeholder. Every canonical/Open Graph/sitemap URL in the app is built
 * from this one constant - once a real domain is chosen, update it here
 * (and nowhere else) and regenerate sitemap.xml (npm run seo:sitemap).
 */
export const SITE_URL = 'https://navon-games.co.il'
export const SITE_NAME = 'נבון משחקים'
export const DEFAULT_OG_IMAGE = '/og-image.png'
